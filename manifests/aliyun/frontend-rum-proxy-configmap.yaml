apiVersion: v1
kind: ConfigMap
metadata:
  name: rum-nginx-config
  namespace: seat-1
data:
  default.conf: |-
    server {
        listen 80;
        location / {
            proxy_pass http://frontend:8080;
        }
    }
