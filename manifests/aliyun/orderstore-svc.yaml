apiVersion: v1
kind: Service
metadata:
  labels:
    app: new-orderstore
  name: new-orderstore
  namespace: seat-1
spec:
  ports:
  - port: 5080
    protocol: TCP
    targetPort: 5080
  selector:
    app: new-orderstore
  type: ClusterIP
