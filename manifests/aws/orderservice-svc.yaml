apiVersion: v1
kind: Service
metadata:
  labels:
    app: new-orderservice
  name: new-orderservice
  namespace: seat-1
spec:
  ports:
  - name: http
    nodePort: 30070
    port: 5070
    protocol: TCP
    targetPort: 5070
  selector:
    app: new-orderservice
  type: NodePort
