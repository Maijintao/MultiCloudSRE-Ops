apiVersion: v1
kind: Service
metadata:
  name: cartservice
  namespace: seat-1
spec:
  ports:
  - name: tcp
    port: 7070
    protocol: TCP
    targetPort: 7070
    nodePort: 30007
  selector:
    app: cartservice
  type: NodePort
