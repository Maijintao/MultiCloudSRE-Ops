apiVersion: v1
kind: Service
metadata:
  name: currencyservice
  namespace: seat-1
spec:
  ports:
  - name: tcp
    port: 7000
    protocol: TCP
    targetPort: 7000
    nodePort: 30009
  selector:
    app: currencyservice
  type: NodePort
